## 📚 **Глава 6: Логический уровень - Чистый анализ**
**Логическая организация данных в PostgreSQL** и как она соотносится с физическим хранением.

---

### **📊 1. Трёхуровневая логическая модель**

#### **Уровень 1: Database (База данных)**
```sql
-- Создание БД
CREATE DATABASE app_db 
  OWNER app_user
  ENCODING 'UTF8'
  LC_COLLATE 'en_US.UTF-8'
  LC_CTYPE 'en_US.UTF-8'
  TABLESPACE fast_ssd
  CONNECTION LIMIT 100;
```

**Ключевые моменты:**
- **Контейнер верхнего уровня** в кластере PostgreSQL
- **По умолчанию 3 БД:** `postgres`, `template0`, `template1`
- **Каждая БД изолирована** - нет кросс-БД запросов (без расширений)
- **Физически:** отдельный каталог в `$PGDATA/base/` по OID

---

#### **Уровень 2: Schema (Схема)**
```sql
-- Создание схемы
CREATE SCHEMA app_schema 
  AUTHORIZATION app_user;

-- Схема по умолчанию: public
-- Системные схемы: pg_catalog, pg_toast, information_schema
```

**Ключевые моменты:**
- **Пространство имён** внутри БД
- **`search_path`** определяет порядок поиска объектов
- **Опасность:** `public` схема доступна всем по умолчанию

**Важнейший параметр - search_path:**
```sql
-- Проверка текущего
SHOW search_path;  -- Обычно: "$user", public

-- Установка
SET search_path TO app_schema, public;

-- ОПАСНОСТЬ: временные таблицы могут перекрывать обычные!
SET search_path TO public, pg_temp;  -- pg_temp будет первым!
```

---

#### **Уровень 3: Relation (Отношение)**
**Типы отношений (relkind в pg_class):**
- `r` - обычная таблица
- `i` - индекс  
- `S` - последовательность
- `v` - представление
- `m` - материализованное представление
- `t` - TOAST таблица
- `f` - внешняя таблица

```sql
-- Просмотр всех отношений
SELECT 
    relname,
    CASE relkind
        WHEN 'r' THEN 'table'
        WHEN 'i' THEN 'index'
        WHEN 'S' THEN 'sequence'
        WHEN 'v' THEN 'view'
        WHEN 'm' THEN 'materialized view'
        WHEN 'c' THEN 'composite type'
        WHEN 't' THEN 'TOAST table'
        WHEN 'f' THEN 'foreign table'
    END as type,
    pg_size_pretty(pg_total_relation_size(oid)) as size
FROM pg_class
WHERE relnamespace = (SELECT oid FROM pg_namespace WHERE nspname = 'public');
```

---

### **🔗 2. Соответствие логического и физического**

#### **Логический уровень → Физический уровень:**
```
Database    → Каталог в $PGDATA/base/{OID}
Schema      → Нет прямого соответствия (метаданные в pg_namespace)
Relation    → Файлы в каталоге БД
```

#### **OID (Object Identifier):**
```sql
-- OID БД
SELECT oid, datname FROM pg_database;

-- OID схемы  
SELECT oid, nspname FROM pg_namespace;

-- OID таблицы
SELECT oid, relname, relfilenode FROM pg_class WHERE relname = 'table_name';

-- Важно: OID постоянен, relfilenode меняется при пересоздании!
```

#### **Файлы таблицы:**
```
24576       - основной файл данных
24576_fsm   - Free Space Map (карта свободного места)
24576_vm    - Visibility Map (карта видимости для VACUUM)
24576.1     - сегмент (если > 1GB)
```

---

### **💾 3. Tablespaces (Табличные пространства)**

#### **Системные tablespaces:**
- **`pg_default`** (`$PGDATA/base/`) - пользовательские данные по умолчанию
- **`pg_global`** (`$PGDATA/global/`) - системные таблицы

#### **Пользовательские tablespaces:**
```sql
-- Создание
CREATE TABLESPACE fast_ssd LOCATION '/mnt/ssd/postgres';

-- Использование
CREATE TABLE orders (...) TABLESPACE fast_ssd;
ALTER TABLE existing_table SET TABLESPACE fast_ssd;  -- Блокирует таблицу!
```

**Когда использовать:**
1. Разные типы дисков (SSD/HDD)
2. Разные RAID массивы
3. Географическое распределение
4. Управление местом на диске

---

### **📦 4. TOAST (The Oversized-Attribute Storage Technique)**

#### **Что такое TOAST:**
- **Автоматический механизм** для хранения больших значений (> 2KB)
- **Разбивает** большие значения на куски
- **Хранит** в отдельной таблице (`pg_toast`)

#### **Стратегии хранения (STORAGE):**
```sql
-- Проверка стратегии
SELECT 
    attname,
    CASE attstorage
        WHEN 'p' THEN 'plain'      -- без TOAST
        WHEN 'e' THEN 'extended'   -- сжатие + out-of-line (по умолчанию)
        WHEN 'm' THEN 'main'       -- in-line если возможно
        WHEN 'x' THEN 'external'   -- out-of-line без сжатия
    END as storage
FROM pg_attribute
WHERE attrelid = 'table_name'::regclass;

-- Изменение стратегии
ALTER TABLE table_name 
ALTER COLUMN large_column 
SET STORAGE EXTERNAL;  -- быстрее чтение, без сжатия
```

#### **Влияние на производительность:**
```sql
-- ПЛОХО: читает все TOAST данные
SELECT * FROM documents;

-- ХОРОШО: читает только нужные колонки
SELECT id, metadata FROM documents;

-- ОЧЕНЬ ХОРОШО: читает часть TOAST данных
SELECT id, substring(large_text from 1 for 100) FROM documents;
```

---

### **🔍 5. Search Path - критически важная настройка**

#### **Проблема:**
```sql
-- ОПАСНАЯ настройка
SET search_path TO public, pg_temp;

-- Атака: злоумышленник создаёт временную таблицу users
CREATE TEMP TABLE users (username text, password text);
INSERT INTO users VALUES ('admin', 'hacked');

-- Ваше приложение делает:
SELECT * FROM users WHERE username = 'admin';
-- Читает из pg_temp, а не из public!
```

#### **Безопасная настройка:**
```sql
-- 1. Явно указывать схемы
SELECT * FROM public.users;

-- 2. Безопасный search_path
SET search_path TO app_schema, public;
-- ИЛИ
ALTER DATABASE app_db SET search_path TO app_schema, public;

-- 3. Отозвать права у public
REVOKE CREATE ON SCHEMA public FROM PUBLIC;
```

---

### **🛠️ 6. Практические рекомендации из главы**

#### **1. Структура именования:**
```sql
-- Плохо
CREATE TABLE "MyTable" ("Column1" INT);  -- Кавычки, регистр

-- Хорошо
CREATE TABLE my_table (column1 INT);     -- Нижний регистр, подчёркивания
```

#### **2. Управление схемами:**
```sql
-- Создавайте отдельные схемы для:
-- app_data    - основные таблицы
-- app_audit   - логи/аудит  
-- app_config  - конфигурация
-- app_temp    - временные объекты

-- Настройка прав
REVOKE ALL ON SCHEMA public FROM PUBLIC;
GRANT USAGE ON SCHEMA app_data TO app_user;
```

#### **3. Мониторинг объектов:**
```sql
-- Размеры объектов
SELECT 
    schemaname,
    relname,
    pg_size_pretty(pg_total_relation_size(schemaname||'.'||relname)) as size,
    pg_size_pretty(pg_total_relation_size(schemaname||'.'||relname) - 
                   pg_relation_size(schemaname||'.'||relname)) as toast_size
FROM pg_stat_user_tables
ORDER BY pg_total_relation_size(schemaname||'.'||relname) DESC
LIMIT 10;
```

---

### **🎯 Ключевые тезисы для собеседования:**

1. **PostgreSQL имеет 3 уровня:** Database → Schema → Relation
2. **OID vs relfilenode:** OID постоянен, relfilenode меняется
3. **TOAST автоматически обрабатывает** большие значения (>2KB)
4. **Search path критически важен для безопасности**
5. **Tablespaces позволяют управлять** физическим расположением данных
6. **`public` схема опасна** - отзывайте права по умолчанию

---

### **⚠️ Самые частые ошибки:**

1. **Использование `SELECT *`** с TOAST-колонками
2. **Небезопасный `search_path`** с `pg_temp` в начале
3. **Создание объектов в `public`** без контроля прав
4. **Игнорирование tablespaces** на разнородном железе
5. **Непонимание разницы** между OID и relfilenode

---

### **📝 Чеклист:**

- [ ] Понимаю трёхуровневую структуру PostgreSQL
- [ ] Знаю как найти физический файл таблицы
- [ ] Настроил безопасный search_path
- [ ] Создал отдельные схемы для разных типов данных
- [ ] Отозвал права у public схемы
- [ ] Знаю когда использовать tablespaces
- [ ] Понимаю влияние TOAST на производительность
