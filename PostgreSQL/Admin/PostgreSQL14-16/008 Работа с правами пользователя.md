## 🔐 **Работа с правами пользователя**

### **🎯 Основная идея:**
**Система аутентификации и авторизации PostgreSQL** - как управлять доступом к данным на разных уровнях.

---

### **👥 1. Роли (Roles) - фундаментальная концепция**

#### **Что такое роль:**
- **Единая сущность** для пользователей и групп
- **Может быть:** пользователем (LOGIN) И/ИЛИ группой
- **Глобальны** для всего кластера (не для отдельной БД)

#### **Создание ролей:**
```sql
-- Пользователь (может логиниться)
CREATE ROLE app_user LOGIN PASSWORD 'secure123';

-- Группа (не может логиниться)
CREATE ROLE read_only_group NOLOGIN;

-- Суперпользователь (ОПАСНО!)
CREATE ROLE admin SUPERUSER;
```

#### **Ключевые атрибуты ролей:**
```sql
-- Проверка атрибутов
SELECT 
    rolname,
    rolsuper,      -- Суперпользователь?
    rolinherit,    -- Наследует права?
    rolcreaterole, -- Может создавать роли?
    rolcreatedb,   -- Может создавать БД?
    rolcanlogin,   -- Может логиниться?
    rolreplication -- Репликация?
FROM pg_roles
WHERE rolname NOT LIKE 'pg_%';
```

---

### **🔑 2. Аутентификация (pg_hba.conf)**

#### **Формат записи:**
```
# TYPE  DATABASE  USER  ADDRESS       METHOD
host    all       all   192.168.1.0/24  scram-sha-256
local   all       all                   peer
```

#### **Методы аутентификации:**
- **`scram-sha-256`** (PostgreSQL 14+) - современный, безопасный
- **`md5`** - устарел, для совместимости
- **`password`** - пароль в открытом виде (ОПАСНО!)
- **`peer`** - по пользователю ОС (только local)
- **`cert`** - SSL сертификаты

#### **Пример безопасной конфигурации:**
```ini
# pg_hba.conf
# Локальные подключения
local   all             postgres                                peer
local   all             all                                     scram-sha-256

# Сетевые подключения (ограниченные)
host    app_db          app_user     192.168.1.0/24            scram-sha-256
host    all             all          10.0.0.0/8                reject  # Блокируем всё остальное

# Никогда не использовать:
# host    all             all          0.0.0.0/0                password
```

---

### **🎫 3. Привилегии (Privileges) - система прав**

#### **Уровни привилегий:**
1. **Роль → Роль** (членство в группах)
2. **Роль → Объект БД** (права на таблицы, функции и т.д.)

#### **Типы привилегий:**
```sql
-- Для таблиц:
SELECT   -- Чтение
INSERT   -- Вставка
UPDATE   -- Обновление
DELETE   -- Удаление
TRUNCATE -- Очистка
REFERENCES -- Внешние ключи
TRIGGER  -- Триггеры

-- Для БД:
CONNECT   -- Подключение
CREATE    -- Создание объектов
TEMPORARY -- Временные таблицы

-- Для схем:
USAGE     -- Использование
CREATE    -- Создание объектов

-- Для функций:
EXECUTE   -- Выполнение
```

---

### **🔄 4. GRANT и REVOKE**

#### **Выдача прав (GRANT):**
```sql
-- Базовые примеры
GRANT SELECT ON TABLE orders TO app_user;
GRANT INSERT, UPDATE ON TABLE users TO manager_role;
GRANT ALL PRIVILEGES ON DATABASE app_db TO admin_role;

-- Права на все таблицы в схеме
GRANT SELECT ON ALL TABLES IN SCHEMA public TO read_only_role;

-- С правом передачи другим
GRANT SELECT ON orders TO app_user WITH GRANT OPTION;
```

#### **Отзыв прав (REVOKE):**
```sql
-- Отзыв конкретных прав
REVOKE INSERT ON users FROM app_user;

-- Отзыв всех прав
REVOKE ALL PRIVILEGES ON orders FROM app_user;

-- Каскадный отзыв (если права были переданы дальше)
REVOKE GRANT OPTION FOR SELECT ON orders FROM app_user CASCADE;
```

---

### **👨‍👩‍👧‍👦 5. Иерархия и наследование прав**

#### **Членство в ролях:**
```sql
-- Добавление в группу
GRANT read_only_group TO app_user;

-- Проверка членства
SELECT 
    rolname,
    ARRAY(SELECT member::regrole 
          FROM pg_auth_members 
          WHERE roleid = pg_roles.oid) as members
FROM pg_roles 
WHERE rolname = 'read_only_group';
```

#### **Наследование прав:**
- **Включено по умолчанию:** `INHERIT`
- **Роль наследует права** всех групп, в которых состоит
- **Псевдороль `PUBLIC`** - все роли автоматически в ней

#### **Опасность PUBLIC:**
```sql
-- По умолчанию:
GRANT ALL ON SCHEMA public TO PUBLIC;  -- ОПАСНО!

-- Исправление:
REVOKE ALL ON SCHEMA public FROM PUBLIC;
REVOKE CREATE ON SCHEMA public FROM PUBLIC;
```

---

### **⚙️ 6. Default Privileges - права по умолчанию**

#### **Проблема:**
```sql
-- Выдаём права на существующие таблицы
GRANT SELECT ON ALL TABLES IN SCHEMA app TO read_only;

-- Но новые таблицы не получат прав автоматически!
CREATE TABLE app.new_table (...);  -- read_only не видит
```

#### **Решение:**
```sql
-- Установка прав по умолчанию
ALTER DEFAULT PRIVILEGES IN SCHEMA app 
GRANT SELECT ON TABLES TO read_only;

ALTER DEFAULT PRIVILEGES FOR ROLE app_owner
GRANT ALL ON TABLES TO admin_role;
```

#### **Проверка default privileges:**
```sql
SELECT 
    schemaname,
    nspname,
    defaclobjtype,
    defaclacl
FROM pg_default_acl a
JOIN pg_namespace n ON n.oid = a.defaclnamespace;
```

---

### **🛡️ 7. Безопасные практики**

#### **1. Принцип наименьших привилегий:**
```sql
-- Не делать:
GRANT ALL ON DATABASE app_db TO app_user;

-- Делать так:
GRANT CONNECT ON DATABASE app_db TO app_user;
GRANT USAGE ON SCHEMA app_schema TO app_user;
GRANT SELECT ON specific_table TO app_user;
```

#### **2. Отдельные роли для разных задач:**
```sql
-- Вместо одной роли "app_user" создаём:
CREATE ROLE app_reader NOLOGIN;
CREATE ROLE app_writer NOLOGIN;
CREATE ROLE app_admin NOLOGIN;

-- И выдаём соответствующие права
GRANT SELECT ON ALL TABLES TO app_reader;
GRANT SELECT, INSERT, UPDATE ON transactions TO app_writer;
GRANT ALL ON ALL TABLES TO app_admin;
```

#### **3. Регулярный аудит прав:**
```sql
-- Скрипт проверки прав
SELECT 
    grantee::regrole as role,
    table_schema,
    table_name,
    string_agg(privilege_type, ', ') as privileges
FROM information_schema.role_table_grants
WHERE grantee != 'postgres'
GROUP BY grantee, table_schema, table_name
ORDER BY role, table_schema, table_name;
```

---

### **🔍 8. Практический пример из книги**

#### **Сценарий:**
```sql
-- 1. Создаём БД и схему
CREATE DATABASE company;
\c company
CREATE SCHEMA hr;

-- 2. Создаём роли
CREATE ROLE hr_manager NOLOGIN;
CREATE ROLE hr_clerk LOGIN PASSWORD 'clerk123';
CREATE ROLE employee LOGIN PASSWORD 'emp123';

-- 3. Настраиваем права схемы
REVOKE ALL ON SCHEMA hr FROM PUBLIC;
GRANT USAGE ON SCHEMA hr TO hr_manager, hr_clerk;

-- 4. Создаём таблицу
CREATE TABLE hr.employees (
    id SERIAL PRIMARY KEY,
    name TEXT,
    salary DECIMAL,
    department TEXT
);

-- 5. Выдаём права
GRANT SELECT ON hr.employees TO employee;
GRANT SELECT, INSERT, UPDATE ON hr.employees TO hr_clerk;
GRANT ALL ON hr.employees TO hr_manager;
GRANT USAGE ON SEQUENCE hr.employees_id_seq TO hr_clerk, hr_manager;

-- 6. Настраиваем default privileges
ALTER DEFAULT PRIVILEGES IN SCHEMA hr
GRANT SELECT ON TABLES TO employee;

-- 7. Добавляем в группы
GRANT hr_clerk TO specific_user;
```

---

### **🎯 Ключевые тезисы для собеседования:**

1. **Роли = пользователи + группы** в одной сущности
2. **pg_hba.conf управляет аутентификацией**, не путать с правами
3. **Принцип наименьших привилегий** - золотое правило
4. **PUBLIC опасен** - отзывайте права по умолчанию
5. **Default privileges** для автоматизации выдачи прав
6. **Наследование прав** через членство в ролях
7. **WITH GRANT OPTION** осторожно - может создать security hole

---

### **⚠️ Самые частые ошибки:**

1. **Использование `md5`** вместо `scram-sha-256` в PostgreSQL 14+
2. **Открытый доступ через `0.0.0.0/0`** в pg_hba.conf
3. **Не отозваны права у `PUBLIC`** на схеме public
4. **Слишком широкие права** (`GRANT ALL` без необходимости)
5. **Забывают про sequences** при выдаче прав на таблицы
6. **Не используют default privileges** для новых объектов
7. **Одна роль для всех** вместо разделения обязанностей

---

### **📝 Чеклист после главы 7:**

- [ ] Понимаю разницу между аутентификацией и авторизацией
- [ ] Настроил безопасный pg_hba.conf с scram-sha-256
- [ ] Отозвал права у PUBLIC на всех схемах
- [ ] Создал отдельные роли для разных уровней доступа
- [ ] Использую default privileges для новых объектов
- [ ] Регулярно делаю аудит выданных прав
- [ ] Применяю принцип наименьших привилегий

---

### **🔧 Полезные команды для повседневной работы:**

```sql
-- Просмотр прав таблицы
\dp table_name

-- Просмотр прав пользователя
SELECT * FROM information_schema.role_table_grants 
WHERE grantee = 'user_name';

-- Копирование прав от одного пользователя другому
-- (нет встроенной команды, нужно скриптовать)

-- Сброс всех прав объекта
REVOKE ALL ON table_name FROM PUBLIC;
REVOKE ALL ON table_name FROM user_name;
```

---

**📌 Заметка:** Система прав PostgreSQL - большая тема. Отмечаю её для детального разбора после обзора книги.
