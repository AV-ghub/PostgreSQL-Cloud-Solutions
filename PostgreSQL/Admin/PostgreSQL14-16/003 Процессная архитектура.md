## 🔄 **Процессная архитектура PostgreSQL (детально)**
### **1. Иерархия процессов**

```
postmaster (PID 1)
├── logger
├── checkpointer
├── background writer
├── wal writer
├── autovacuum launcher
│   └── autovacuum worker (сколько нужно)
├── stats collector
├── archiver (если включен)
└── client backends (по одному на подключение)
```

#### **Детальное описание каждого процесса:**

**1. Postmaster (родительский процесс):**
```bash
# Идентификация
ps aux | grep "postgres: logger" | head -1
```
- **Назначение:** Принимает TCP подключения, порождает дочерние процессы
- **PID:** Всегда первый процесс PostgreSQL
- **Умирает при:** kill -9, системном сбое
- **Рестарт:** Требует перезапуска всего кластера

**2. Logger:**
- Пишет в `log_directory` (по умолчанию `pg_log/`)
- Настраивается через `log_*` параметры
- **Важно:** Не блокирует основные операции

**3. Checkpointer:**
```sql
-- Настройки checkpoint
SELECT name, setting, unit 
FROM pg_settings 
WHERE name LIKE '%checkpoint%';
```
- **Назначение:** Записывает "грязные" страницы на диск
- **Триггеры срабатывания:**
  1. `checkpoint_timeout` (по умолчанию 5 мин)
  2. `max_wal_size` (по умолчанию 1GB)
  3. Вручную: `CHECKPOINT;`
- **Производительность:** Должен завершаться быстро

**4. Background Writer:**
- **Назначение:** Постепенно записывает грязные страницы между checkpoint'ами
- **Цель:** Уменьшить пиковую нагрузку во время checkpoint
- **Настройки:** `bgwriter_delay`, `bgwriter_lru_maxpages`

**5. WAL Writer:**
- **Назначение:** Асинхронная запись WAL на диск
- **Критически важно:** Для durability (прочность данных)
- **Настройки:** `wal_writer_delay`, `wal_writer_flush_after`

**6. Autovacuum Launcher/Worker:**
```sql
-- Мониторинг autovacuum
SELECT 
    datname,
    count(*) as workers,
    array_agg(query) as queries
FROM pg_stat_activity 
WHERE backend_type = 'autovacuum worker'
GROUP BY datname;
```
- **Launcher:** Запускает workers по необходимости
- **Worker:** Выполняет VACUUM и ANALYZE
- **Лимит:** `autovacuum_max_workers` (по умолчанию 3)

**7. Stats Collector:**
- Собирает статистику для `pg_stat_*` представлений
- Можно отключить: `track_counts = off` (не рекомендуется)

**8. Archiver (опционально):**
- Архивирует WAL файлы
- Требуется для PITR (Point-in-Time Recovery)

---

### **2. Backend процессы (клиентские соединения)**

#### **Жизненный цикл подключения:**
```sql
-- 1. Клиент подключается → postmaster создает новый процесс
-- 2. Процесс выполняет аутентификацию
-- 3. Выполняет запросы
-- 4. При разрыве соединения - процесс завершается

-- Просмотр backend процессов
SELECT 
    pid,
    usename,
    application_name,
    client_addr,
    state,
    query_start,
    query
FROM pg_stat_activity 
WHERE backend_type = 'client backend'
AND state != 'idle';
```

#### **Память backend процесса:**
```
Каждый backend имеет:
├── work_mem (для сортировок, hash joins)
├── maintenance_work_mem (для VACUUM, CREATE INDEX)
├── temp_buffers (для временных таблиц)
└── Локальные структуры данных
```

---

### **3. Мониторинг и управление процессами**

#### **Практические команды:**
```bash
# 1. Просмотр всех процессов PostgreSQL
ps aux --forest | grep postgres

# 2. Дерево процессов
pstree -p $(head -1 /var/lib/postgresql/14/main/postmaster.pid)

# 3. Потребление памяти
sudo pmap -x $(pidof postgres) | tail -1
```

#### **Мониторинг через SQL:**
```sql
-- Полная картина процессов
WITH process_groups AS (
    SELECT 
        backend_type,
        COUNT(*) as count,
        SUM(
            CASE WHEN state = 'active' 
            THEN 1 ELSE 0 END
        ) as active,
        string_agg(
            CASE WHEN state = 'active' 
            THEN query ELSE NULL END, 
            ' | '
        ) as active_queries
    FROM pg_stat_activity
    GROUP BY backend_type
)
SELECT * FROM process_groups
ORDER BY count DESC;

-- Детализация по клиентским подключениям
SELECT 
    datname,
    COUNT(*) as total_connections,
    COUNT(*) FILTER (WHERE state = 'active') as active,
    COUNT(*) FILTER (WHERE state = 'idle') as idle,
    COUNT(*) FILTER (WHERE state = 'idle in transaction') as idle_in_xact,
    COUNT(*) FILTER (WHERE state = 'fastpath function call') as fastpath,
    COUNT(*) FILTER (WHERE state = 'disabled') as disabled
FROM pg_stat_activity
WHERE backend_type = 'client backend'
GROUP BY datname
ORDER BY total_connections DESC;
```

#### **Управление процессами:**
```sql
-- Завершение конкретного процесса
SELECT pg_terminate_backend(pid);

-- Завершение всех процессов, кроме своих
SELECT pg_terminate_backend(pid)
FROM pg_stat_activity
WHERE pid <> pg_backend_pid()
AND backend_type = 'client backend'
AND state = 'idle'
AND (now() - state_change) > interval '10 minutes';

-- Принудительное завершение (SIGKILL)
SELECT pg_cancel_backend(pid);  -- Более мягкое
-- pg_terminate_backend - SIGTERM
-- Для SIGKILL нужно использовать системные команды
```

---

### **4. Настройка процессов**

#### **Критические параметры:**
```sql
-- Параметры процессов
SELECT 
    name, 
    setting, 
    unit,
    short_desc,
    context
FROM pg_settings
WHERE name IN (
    'max_connections',
    'superuser_reserved_connections',
    'autovacuum_max_workers',
    'max_worker_processes',
    'max_parallel_workers',
    'wal_writer_delay',
    'bgwriter_delay'
);
```

#### **Рекомендации по настройке:**
```ini
# postgresql.conf
max_connections = 100                    # Начинать с этого
superuser_reserved_connections = 3       # Для администрации
autovacuum_max_workers = 3               # По умолчанию
max_worker_processes = 8                 # Для параллельных запросов
max_parallel_workers_per_gather = 2      # Параллелизм
wal_writer_delay = 200ms                 # Частота записи WAL
bgwriter_delay = 200ms                   # Фоновая запись
```

---

### **5. Проблемы и решения**

#### **Проблема 1: Слишком много процессов**
```sql
-- Найти "виновника"
SELECT 
    usename,
    client_addr,
    COUNT(*) as connections,
    array_agg(application_name) as apps
FROM pg_stat_activity
WHERE backend_type = 'client backend'
GROUP BY usename, client_addr
HAVING COUNT(*) > 5
ORDER BY connections DESC;
```
**Решение:** Использовать пул соединений (PgBouncer, Pgpool-II)

#### **Проблема 2: Долгие запросы**
```sql
-- Найти долгие запросы
SELECT 
    pid,
    now() - query_start as duration,
    query,
    wait_event_type,
    wait_event
FROM pg_stat_activity
WHERE state = 'active'
AND (now() - query_start) > interval '5 minutes'
ORDER BY duration DESC;
```
**Решение:** `SET statement_timeout = '10min';`

#### **Проблема 3: Autovacuum не справляется**
```sql
-- Проверка автовакуума
SELECT 
    schemaname,
    relname,
    n_live_tup,
    n_dead_tup,
    round(100.0 * n_dead_tup / (n_live_tup + 1), 2) as dead_ratio,
    last_autovacuum,
    last_autoanalyze
FROM pg_stat_user_tables
WHERE n_dead_tup > 1000
ORDER BY dead_ratio DESC;
```
**Решение:** Увеличить `autovacuum_max_workers`, настроить `autovacuum_vacuum_scale_factor`

---

### **6. Практическое задание**

#### **Задание 1: Анализ текущей архитектуры**
```bash
# 1. Найдите все процессы PostgreSQL
pgrep -a postgres

# 2. Постройте дерево процессов
ps axfo pid,ppid,command | grep -A5 -B5 postgres

# 3. Проверьте использование памяти
for pid in $(pgrep postgres); do
    echo "PID $pid:"
    pmap $pid | tail -1
done
```

#### **Задание 2: Мониторинг в реальном времени**
```sql
-- 1. Создайте представление для мониторинга
CREATE VIEW process_monitor AS
SELECT 
    pid,
    backend_type,
    CASE backend_type
        WHEN 'client backend' THEN 
            usename || ' @ ' || COALESCE(client_addr::text, 'local')
        ELSE backend_type
    END as description,
    state,
    CASE 
        WHEN query_start IS NOT NULL 
        THEN now() - query_start 
        ELSE NULL 
    END as query_age,
    LEFT(query, 50) as query_preview
FROM pg_stat_activity
ORDER BY 
    CASE backend_type
        WHEN 'client backend' THEN 2
        ELSE 1
    END,
    query_age DESC NULLS LAST;

-- 2. Используйте его
SELECT * FROM process_monitor;
```

#### **Задание 3: Стресс-тест процессов**
```bash
# Создайте нагрузку (в отдельном терминале)
for i in {1..50}; do
    psql -c "SELECT pg_sleep(10);" &
done

# Мониторьте в реальном времени
watch -n1 "psql -c 'SELECT COUNT(*), state FROM pg_stat_activity WHERE backend_type = \"client backend\" GROUP BY state;'"
```

---

### **🎯 Ключевые тезисы для собеседования:**

1. **Каждое подключение = отдельный процесс ОС** (не поток)
2. **Postmaster управляет всеми процессами** и принимает подключения
3. **Autovacuum критически важен** для поддержания производительности
4. **Checkpoint должен завершаться быстро** - иначе блокировка
5. **Мониторинг процессов обязателен** в production

---

## ❓ **Вопросы для самопроверки:**

1. Что произойдет, если убить процесс postmaster?
2. Как отличить autovacuum worker от обычного подключения?
3. Почему нельзя иметь тысячи подключений без пулера?
4. Как настроить autovacuum для очень большой таблицы?
5. Что такое "idle in transaction" и почему это плохо?

---
